# SEO & Discovery Rules (إلزامي)

هذا ترحيل لمتجر حي مفهرس. فقدان الترتيب أغلى من تأخّر الإطلاق. قراءة إلزامية مع `AI_DESIGN_RULES.md` قبل أي مسار أو صفحة.

## E0. قبل الكود
1. صدّر كل روابط سلة الحية (منتجات، تصنيفات، علامات، صفحات، مدونة).
2. جدول `old_url → new_url` في `seo/redirect-map.csv`.
3. خط أساس Search Console (انطباعات/نقرات لكل رابط).
4. جمّد بنية الروابط (E1). **لا مسار يُبنى قبل وجود خريطة التحويل.**

## E1. بنية الروابط — تُقرَّر مرة
```
/                          الرئيسية
/categories/[slug]         تصنيف
/products/[slug]           منتج
/search?q=                 بحث (noindex)
/cart /checkout /account   noindex, nofollow
```
Slugs ثابتة (إعادة التسمية لا تغيّر slug). عربي أو لاتيني بلا خلط. أحرف صغيرة وشرطات، بلا `/` ختامية، بلا معرّفات إلا للتفرّد. مضيف canonical واحد (`www` أو بدونه) مفروض بتحويل.

## E2. التحويلات (301)
كل رابط في الخريطة → 301 (لا 302، لا 404 ناعم، لا الرئيسية). لا سلاسل (`A→C` لا `A→B→C`). تبقى ≥ 12 شهراً. المحذوف/المنتهي: 301 لأقرب بديل أو التصنيف الأب؛ 410 فقط بلا بديل. لا 404 أبداً.

## E3. البيانات الوصفية — كل مسار
`generateMetadata` إلزامي. `title` فريد عربي ≤ 60 مع لاحقة العلامة. `description` فريد ≤ 160 من بيانات المنتج (لا قوالب مكررة). `canonical` ذاتي على كل صفحة قابلة للفهرسة. `og:*` + `twitter:card`. `lang="ar"` `dir="rtl"`.

## E4. البيانات المهيكلة (JSON-LD خادمياً)
| الصفحة | المخططات |
|---|---|
| منتج | `Product` + `Offer` (السعر، SAR، التوفر، الحالة) + `BreadcrumbList` |
| تصنيف | `ItemList` + `BreadcrumbList` |
| الرئيسية | `Organization` + `WebSite` (مع `SearchAction`) |
| صفحة بتقييمات | `AggregateRating` فقط إن وُجدت تقييمات حقيقية |
تُرسم في HTML من الخادم. `Offer.availability` يعكس المخزون الفعلي. لا ترميز لبيانات غير موجودة على الصفحة.

## E5. المحتوى المكرر
أي رابط بمعامل فلترة/ترتيب → `noindex, follow` + canonical للتصنيف النظيف. الصفحة 1 canonical لنفسها؛ الصفحات 2+ ذاتية وقابلة للفهرسة مع روابط `<a href>` حقيقية. لا canonical للصفحة 2+ نحو الصفحة 1. البحث الداخلي `noindex`. منتج في عدة تصنيفات = رابط canonical واحد. معاملات الجلسة/التتبع/`utm_*` لا تولّد نسخة قابلة للفهرسة.

## E6. الزحف
`sitemap.xml` ديناميكي مقسّم (products, categories, pages) بـ `lastmod` دقيق ويستثني `noindex`. `robots.txt` يمنع `/cart`, `/checkout`, `/account`, `/api`, البحث. كل منتج قابل للوصول عبر `<a href>` من الرئيسية. شبكة التصنيف تُرسم خادمياً؛ التمرير اللانهائي له بديل مرقّم. Breadcrumbs روابط حقيقية.

## E7. أهداف الأداء
LCP < 2.5s (البطل وأول صورة منتج: `loading="eager"`/preload، لا كسول فوق الطيّة). CLS < 0.1 (`aspect-ratio` محجوز في ProductCard واللافتات والهيدر اللاصق والتنقّل السفلي). INP < 200ms. الخط: IBM Plex Sans Arabic مقتطع (عربي + لاتيني)، `font-display: swap`، محمّل مسبقاً. صور بصيغ حديثة و`sizes` صحيحة.

## E8. العربية والإقليمية
المحتوى والبيانات الوصفية والمهيكلة بالعربية؛ الأسعار SAR. `hreflang` فقط عند وجود لغة ثانية فعلاً. أوصاف فريدة (لا نصوص موردين). بيانات `Organization` متسقة مع السجل التجاري.

## E9. التحليلات
طبقة وسوم واحدة، خادمية حيث أمكن، لا تحجب الرسم. الموافقة لا تخفي المحتوى عن الزواحف. Search Console + Bing قبل الإطلاق، وإرسال sitemap يوم الإطلاق ومراقبة يومية أسبوعين.

## E10. قائمة ما قبل الإطلاق
- [ ] `redirect-map.csv` كامل ومختبر — كل رابط قديم 301، صفر سلاسل
- [ ] خط أساس Search Console
- [ ] بنية الروابط مجمّدة؛ slugs ثابتة
- [ ] `generateMetadata` على كل مسار؛ لا عناوين/أوصاف مكررة
- [ ] canonicals ذاتية
- [ ] JSON-LD صالح (منتج، تصنيف، رئيسية)
- [ ] فلاتر/ترتيب/بحث `noindex`؛ الترقيم قابل للفهرسة
- [ ] `sitemap.xml` + `robots.txt`
- [ ] كل منتج قابل للوصول بروابط
- [ ] LCP < 2.5s، CLS < 0.1 على الجوال
- [ ] الخط العربي مقتطع ومحمّل مسبقاً
- [ ] `ProductCard` يحجز مساحة الصورة
- [ ] مصفوفة الأقسام محلولة خادمياً
- [ ] السلة/الدفع/الحساب خارج الفهرس
