# AI Design Rules (إلزامي)

تعليمات دائمة تعلو على أي طلب يقول "ابنِ صفحة جميلة".

> ابنِ الصفحة بنظام التصميم الموجود. أعد استخدام المكوّنات والـ variants. لا تُدخل قيماً بصرية جديدة إلا إن كانت قابلة لإعادة الاستخدام وأُضيفت إلى الرموز أولاً.

## B0. توقّف قبل الإنشاء
قبل أي Component · Variant · Token · Color · Layout pattern جديد:
1. هل يوجد حل قائم؟ 2. هل يعمل مكوّن موجود كما هو؟ 3. هل يحلّه variant موثّق؟
4. فقط إن اختلفت المسؤولية فعلاً → أصغر تجريد قابل لإعادة الاستخدام، وثّقه في `DESIGN_SYSTEM.md` في نفس التغيير.

## B1. قبل الكتابة
اقرأ `DESIGN_SYSTEM.md` ثم هذا الملف، افحص `src/components/**` و`src/design-system/variants.ts`، وابحث قبل أن تنشئ.

## B2. التنسيق
- رموز دلالية فقط (`semantic.css` / Tailwind: `bg-surface`, `text-ink`, `text-ink-secondary`, `border-border`, `bg-brand`, `rounded-card`, `rounded-button`, `shadow-card`). رموز foundation داخل `semantic.css` فقط.
- لا ألوان ولا مسافات ولا أحجام خط ولا استدارة ولا ظلال ثابتة. لا قيم عشوائية (`p-[13px]`, `#3B7CF6`).
- CSS محلي مسموح فقط لـ: `display`, `position`, `flex`, `grid`, `transform`, `overflow`, `opacity`.

## B3. المكوّنات
مكوّن = مسؤولية واحدة · أعد الاستخدام قبل الإنشاء · variant بدل مكوّن مكرر · لا variant غير مسجّل · المكوّن لا يعرف الثيم · لا wrappers بلا قيمة · كل مكوّن جديد مع `CONTRACT.md`.

## B4. التخطيط
Mobile-first · RTL-first (خصائص منطقية، لا `left/right`) · عربي أولاً · HTML دلالي · تسلسل بصري · لا أغلفة زائدة.

## B5. الصفحات
تركيب أقسام ومكوّنات موجودة · لا تكرار UI خاص بصفحة · لا CSS قابل لإعادة الاستخدام داخل ملف صفحة · `<h1>` واحد.

## B6. الأداء
Server Components افتراضياً · Client فقط للتفاعل · صور محسّنة بـ `aspect-ratio` محجوز · لا مكتبات حركة ثقيلة · لا محتوى مهم خلف JavaScript.

## B7. لا تخترع
ألواناً · مسافات · أحجام خط · استدارة · ظلال · variants · ثيمات — إلا إن كانت قابلة لإعادة الاستخدام **وأُضيفت للنظام أولاً**.

## B8. قائمة ما قبل الالتزام
- [ ] لا ألوان/مسافات/استدارة/ظلال/أحجام ثابتة
- [ ] رموز دلالية فقط داخل المكوّنات
- [ ] لا variant خارج السجل
- [ ] لا مكوّن مكرر
- [ ] المكوّن لا يعرف الثيم
- [ ] يعمل في RTL
- [ ] مُختبر على جوال / لوحي / مكتب
- [ ] لوحة مفاتيح + تركيز مرئي
- [ ] HTML دلالي + ترتيب عناوين صحيح
- [ ] Server Component ما لم يلزم التفاعل
- [ ] `DESIGN_SYSTEM.md` مُحدَّث إن وُسِّع النظام
